Quero preparar uma apresentação técnica sobre o projeto atual de automação e healing de testes Playwright.

Preciso que você analise **o código-fonte, arquivos de configuração, documentação, prompts/diretrizes dos agentes, scripts e estrutura de pastas existentes no projeto** e produza um levantamento técnico baseado exclusivamente no que está implementado/documentado atualmente.

Não quero uma explicação genérica sobre agentes de IA ou Playwright. Quero entender e apresentar **a arquitetura real deste projeto**, com detalhes suficientes para uma apresentação técnica, mas sem entrar em explicações excessivamente profundas de implementação.

A apresentação terá dois slides principais:

## SLIDE 1 — TEST ORCHESTRATOR

Analise especificamente o Orchestrator e explique:

1. **Objetivo**

   * Qual problema ele resolve?
   * Em que cenário ele deve ser utilizado?
   * Qual é a diferença entre simplesmente executar a suíte e utilizar o Orchestrator?

2. **Fluxo técnico**
   Explique o fluxo completo, desde a seleção das pastas/testes até o resultado final.
   Mostre, em ordem:

   * como os testes são selecionados;
   * como são executados;
   * como as falhas são coletadas;
   * como as falhas são analisadas/classificadas;
   * como os testes são agrupados;
   * como os batches/lotes são criados;
   * como esses batches são preparados para o processo de healing;
   * como o estado/progresso da execução é controlado;
   * como retries, limites ou controles de execução funcionam, caso existam.

3. **Estrutura técnica**
   Identifique:

   * principais arquivos envolvidos;
   * scripts;
   * módulos;
   * funções/classes relevantes;
   * arquivos de configuração;
   * formatos de entrada e saída;
   * arquivos temporários ou de estado;
   * como os componentes se comunicam.

Não precisa explicar cada função individualmente. Quero os componentes importantes para entender a arquitetura.

4. **Estratégia de batching**
   Explique tecnicamente como o Orchestrator decide o agrupamento das falhas.

   Quero entender:

   * quais informações são usadas para agrupar;
   * se o agrupamento considera arquivo, erro, causa, spec, locator ou outros critérios;
   * qual é a granularidade esperada dos batches;
   * como um batch é estruturado;
   * por que esse agrupamento é útil para o Healer;
   * como isso reduz contexto/repetição e facilita o processo de diagnóstico.

Importante: batches normalmente são utilizados com múltiplos testes por lote (por exemplo, 4–5 testes), então não trate batch size = 1 como comportamento normal, a menos que o código indique isso.

5. **Integração com o Healer**
   Explique exatamente como o Orchestrator prepara e encaminha o contexto para o processo de healing.

   Mostre a separação de responsabilidades:

   * o que pertence ao Orchestrator;
   * o que pertence ao Healer;
   * quais dados são passados entre eles.

6. **Controles e segurança**
   Identifique mecanismos existentes para:

   * limitar retries;
   * controlar sessões/turnos;
   * persistir estado;
   * evitar execução desnecessária;
   * controlar falhas;
   * manter intervenção/aprovação humana quando aplicável.

7. **Pontos técnicos para destacar no slide**
   No final, gere de 5 a 8 bullets curtos com os principais pontos técnicos que eu deveria colocar visualmente no slide.

---

# SLIDE 2 — PLAYWRIGHT TEST HEALER

Agora analise especificamente o agente Healer e suas diretrizes/documentação.

Quero uma explicação técnica de **como o agente pensa e trabalha durante o processo de healing**, baseada no agent `.md`, prompts, configurações e código realmente existente.

Cubra:

1. **Objetivo e responsabilidade**

   * Qual problema o Healer resolve?
   * O que ele recebe como entrada?
   * O que ele deve produzir?
   * O que ele NÃO deve fazer?

2. **Diretrizes do agente**
   Extraia e organize as principais regras/instruções do agente.

   Principalmente:

   * comportamento esperado;
   * investigação antes de qualquer alteração;
   * uso das ferramentas disponíveis;
   * critérios para diagnóstico;
   * tratamento de diferentes tipos de falha;
   * validação;
   * limites do agente;
   * Human-in-the-Loop.

Não copie o `.md` inteiro. Transforme as diretrizes em uma explicação técnica estruturada.

3. **Como o agente lê e entende o projeto**
   Explique como ele utiliza:

   * arquivos de teste;
   * Page Objects;
   * configurações;
   * estrutura do projeto;
   * evidências da execução;
   * DOM/snapshot;
   * URL/navegação;
   * locators;
   * console;
   * network;
   * estado da página;
   * ferramentas Playwright/MCP;
   * outros artefatos disponíveis.

Explique a ordem lógica da investigação quando isso estiver definido na documentação/código.

4. **Processo de diagnóstico**
   Descreva o ciclo técnico:

   failure
   → investigação
   → coleta de evidências
   → identificação da causa raiz
   → definição da correção
   → geração da proposta
   → aprovação humana
   → aplicação
   → validação

   Ajuste esse fluxo caso o código/documentação atual tenha alguma diferença.

5. **Como o agente trata os erros**
   Identifique os principais tipos de problema que ele consegue investigar, por exemplo:

   * locator/elemento alterado;
   * URL/navigation;
   * comportamento da página;
   * timing/synchronization;
   * conteúdo;
   * outros casos realmente contemplados pelo projeto.

   Não invente categorias que não estejam presentes no projeto.

6. **Regra de alteração de código**
   Explique claramente o mecanismo atual de proposta de correção.

   É importante destacar que o agente **não deve editar diretamente os arquivos-fonte**.

   Explique:

   * como a correção é proposta;
   * formato utilizado (`.patch` ou outro, conforme o projeto);
   * onde a proposta é armazenada;
   * como ocorre a revisão/aprovação humana;
   * como a alteração é posteriormente aplicada;
   * como a solução é validada.

7. **Arquitetura das ferramentas**
   Identifique como o Gemini CLI, Playwright/Playwright MCP e o agente se relacionam.

   Quero uma explicação simples da responsabilidade de cada camada.

8. **Como obter melhores resultados do agente**
   Com base nas próprias diretrizes do projeto, explique:

   * qual contexto deve ser fornecido;
   * como apresentar uma falha;
   * quais informações são importantes;
   * como estruturar um pedido;
   * quais comportamentos devem ser evitados;
   * como o agente deve ser conduzido para produzir um diagnóstico confiável.

Não quero "prompt engineering" genérico. Quero as melhores práticas específicas derivadas deste projeto.

9. **Ganhos técnicos**
   Identifique os ganhos observáveis ou esperados pela arquitetura atual, separando:

   * ganho de manutenção;
   * ganho de investigação/diagnóstico;
   * ganho de produtividade;
   * ganho de organização das falhas;
   * ganho de segurança/controle por Human-in-the-Loop.

Não invente métricas. Se houver métricas/resultados registrados no projeto, apresente-os separadamente como resultados do POC.

10. **Pontos técnicos para destacar no slide**
    Gere de 6 a 10 bullets curtos, próprios para colocar visualmente no slide.

---

# FORMATO DA RESPOSTA

Estruture a resposta exatamente nesta ordem:

### 1. Executive technical summary

Um resumo curto da arquitetura completa.

### 2. Slide 1 — Orchestrator

* Objetivo
* Fluxo
* Arquitetura
* Batching
* Integração com Healer
* Controles
* 5–8 bullets finais para o slide

### 3. Slide 2 — Healer

* Objetivo
* Diretrizes
* Leitura de contexto
* Diagnóstico
* Tratamento de erros
* Geração de patch/proposta
* Human-in-the-Loop
* Arquitetura das ferramentas
* Como utilizar melhor
* Ganhos
* 6–10 bullets finais para o slide

### 4. Technical architecture diagram description

Descreva em texto um diagrama que eu poderia transformar em uma imagem no PowerPoint, mostrando:

Test Selection
→ Orchestrator
→ Test Execution
→ Failure Collection
→ Failure Grouping / Batching
→ Healer
→ Playwright/MCP Investigation
→ Root Cause
→ Patch Proposal
→ Human Approval
→ Apply
→ Validation

Ajuste os componentes conforme a arquitetura real encontrada no projeto.

### 5. Evidence from the project

Para cada afirmação técnica importante, indique **qual arquivo, documentação, função ou componente do projeto comprova aquela informação**.

Diferencie claramente:

* comportamento realmente implementado;
* comportamento definido apenas nas diretrizes/documentação;
* comportamento que é apenas uma possibilidade ou próximo passo.

### Regras importantes

* Baseie-se no projeto atual, não em suposições.
* Não invente funcionalidades.
* Não proponha uma arquitetura nova; primeiro descreva a arquitetura existente.
* Não altere nenhum arquivo.
* Não sugira mudanças no agente durante esta análise.
* Não remova nem simplifique nenhuma diretriz existente.
* Se houver alguma inconsistência entre código e documentação, destaque explicitamente.
* Se alguma informação não puder ser confirmada no projeto, escreva **"não confirmado no código/documentação"**.
* Mantenha o nível técnico adequado para uma apresentação para liderança de tecnologia: arquitetura, fluxo, componentes, responsabilidades, regras e decisões técnicas, sem transformar a resposta em uma documentação de código linha a linha.
* Dê preferência a diagramas textuais, fluxos e bullets que possam ser diretamente convertidos em slides.
